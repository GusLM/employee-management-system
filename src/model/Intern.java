package model;

import java.math.BigDecimal;

public class Intern extends Employee {

    public Intern(String name, String cpf, String email, BigDecimal baseSalary) {
        super(name, cpf, email, baseSalary);
    }
}
